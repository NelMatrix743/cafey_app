import "package:flutter/material.dart";

const String assetDirPath = "assets/icons/";

// consts endpoints for icon assets
const homeOutlinedIcon = AssetImage("${assetDirPath}home_outlined.png");
const homeFilledIcon = AssetImage("${assetDirPath}home_filled.png");

const coffeeShopOutlinedIcon = AssetImage(
  "${assetDirPath}coffee_shop_outlined.png",
);
const coffeeShopFilledIcon = AssetImage(
  "${assetDirPath}coffee_shop_filled.png",
);

const shoppingCartOutlinedIcon = AssetImage(
  "${assetDirPath}shopping_cart_outlined.png",
);
const shoppingCartFilledIcon = AssetImage(
  "${assetDirPath}shopping_cart_filled.png",
);

// long black
const longBlackNormalAssetIcon = AssetImage(
  "${assetDirPath}black_coffee_normal.png",
);
const longBlackSharpAssetIcon = AssetImage(
  "${assetDirPath}black_coffee_sharp.png",
);

// espresso
const espressoNormalAssetIcon = AssetImage(
  "${assetDirPath}espresso_normal.png",
);
const espressoSharpAssetIcon = AssetImage("${assetDirPath}espresso_sharp.png");

// latte
const latteNormalAssetIcon = AssetImage("${assetDirPath}latte_normal.png");
const latteSharpAssetIcon = AssetImage("${assetDirPath}latte_sharp.png");

// iced coffee
const icedCoffeeNormalAssetIcon = AssetImage(
  "${assetDirPath}ice_coffee_normal.png",
);
const icedCoffeeSharpAssetIcon = AssetImage(
  "${assetDirPath}ice_coffee_sharp.png",
);

// toast widget icons
const toastAlertFilledIcon = AssetImage("${assetDirPath}alert_filled.png");
const toastSuccessFilledIcon = AssetImage("${assetDirPath}success_filled.png");
const toastCloseFilledIcon = AssetImage("${assetDirPath}close_filled.png");

// slidable widget icon
const deleteTileFilledIcon = AssetImage("${assetDirPath}delete_filled.png");
